const DashboardPage = () => {
  return (
    <div className="p-5">
      {/* <Select
        style={{ width: 200 }}
        placeholder="Select an option"
        options={[
          { value: "option1", label: "Option 1" },
          { value: "option2", label: "Option 2" },
          { value: "option3", label: "Option 3" },
        ]}
        onSelect={(value) => {
          // Handle only mouse-based selection
          handleMouseSelect(value as string);
        }}
        onKeyDown={preventKeyboardSelection} // Keyboard selection ko prevent karein
      /> */}
      dashboard
    </div>
  );
};

export default DashboardPage;
